import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Café Consume - Your Neighborhood Coffee Sanctuary in Chicago",
  description: "Exceptional coffee, warm conversations, and unforgettable mornings at Café Consume. Visit us at 3452 S Western Ave, Chicago, IL 60608.",
  keywords: "coffee shop, cafe, Chicago, breakfast, cafe consume, western ave, neighborhood cafe",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
